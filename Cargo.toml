[package]
name = "plum_runner"
version = "0.1.0"
edition = "2021"

[workspace]
members = ["kernel"]
resolver = "2"

[build-dependencies]
bootloader = "0.11"
kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }
